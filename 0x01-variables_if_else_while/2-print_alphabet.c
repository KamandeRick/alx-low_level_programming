#include <stdio.h>

/**
 * main - print lowercase alphabet with new line
 * Return: Always 0
 */

int main(void)
{
	char lowerCase;

	for (lowerCase = 'a'; lowerCasee <= z; lowerCase++)
	{
		putchar(laowerCase);
	}
	putchar('\n');
	return (0);

}
